<Window x:Class="_2048_game.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="600" Width="600">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Game title, score, best score, and new game button -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="5">
            <TextBlock Text="2048" FontSize="40" FontWeight="Bold" Margin="10"/>
            <TextBlock x:Name="ScoreTextBlock" Text="Score: 0" FontSize="20" Margin="10"/>
            <TextBlock x:Name="BestScoreTextBlock" Text="Best: 0" FontSize="20" Margin="10"/>
            <Button x:Name="NewGameButton" Content="New Game" Click="NewGameButton_Click" Margin="10"/>
        </StackPanel>

        <!-- Game grid -->
        <Grid x:Name="HerniPole" ShowGridLines="False" Grid.Row="1" Margin="15">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
        </Grid>
    </Grid>
</Window>